This folder contains sample CSV files generated by the scraper for demonstration and tests. Run `python -m gnn_rl.scraper.main --ns <namespace> --once` to collect actual metrics from Linkerd-viz and Prometheus. The resulting files `edges.csv` and `node_features.csv` will appear here.
